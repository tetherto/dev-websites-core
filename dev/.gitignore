# Dev playground artifacts (regenerated by `npm run dev:cms`)
.next/
media/
next-env.d.ts
*.tsbuildinfo
# Next auto-installs its TS type stubs here (dev/package.json is the project root)
node_modules/
package-lock.json
# Regenerated on every `npm run cms:importmap` / `npm run dev:cms`
app/(payload)/admin/importMap.js
